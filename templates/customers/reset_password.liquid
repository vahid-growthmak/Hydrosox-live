<section class="hs-scheme-paper">
  <div class="hs-shell hs-pace-base hs-auth">
    <h1 class="hs-h2 hs-balance hs-page__title">{{ 'customer.reset_password.title' | t }}</h1>
    {%- form 'reset_customer_password', class: 'hs-auth__form' -%}
      {%- if form.errors -%}
        <p class="hs-form-error" role="alert">{{ form.errors | default_errors }}</p>
      {%- endif -%}
      <label class="hs-eyebrow hs-muted" for="hs-reset-pw">{{ 'customer.reset_password.password' | t }}</label>
      <input class="hs-field" id="hs-reset-pw" type="password" name="customer[password]" autocomplete="new-password">
      <label class="hs-eyebrow hs-muted" for="hs-reset-pw2">{{ 'customer.reset_password.password_confirm' | t }}</label>
      <input class="hs-field" id="hs-reset-pw2" type="password" name="customer[password_confirmation]" autocomplete="new-password">
      {% render 'hs-button', label: 'customer.reset_password.submit' | t, style: 'primary', type: 'submit', full: true %}
    {%- endform -%}
  </div>
</section>
